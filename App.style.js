import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    flex: 1,
    margin: 15,
  },
  imgBackground: { flex: 1, backgroundColor: '#000' },
  img: { opacity: 0.75 },
});
