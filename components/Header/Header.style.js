import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  backButton: {
    width: 30,
  },
  textContainer: {
    flex: 1,
    alignItems: 'center',
    marginRight: 30,
  },
  subTitle: {
    fontSize: 20,
  },
});
